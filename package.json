{
  "name": "doppelkopf",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev:server": "bun run --cwd server dev",
    "dev:client": "bun run --cwd client dev"
  },
  "workspaces": [
    "shared",
    "client",
    "server"
  ],
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
